# (λx : (Bool * Bool) -> let z*u = x in z) < true * false >
# (\x : (Bool * Bool) -> \y : Bool -> < x * y >) 
(\x : Bool -> \y : Bool -> < x * y >) true false

# FIX: STLLC "Bool -o Bool -o ((Bool * 1) * Bool)"

# let main = let x = {{ ... }} in {{ Bool }}
