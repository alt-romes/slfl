# let f = (\fc : (Bool -o Bool) -> (\x : Bool -o if x then true else fc true));

# let fix = (λf -> (λx -> f (λy -> x x y)) (λx -> f (λy -> x x y)));

# let main = (\x : + {red: Bool;} -o x);

let main = let ! id = ! (\x -o x) in id id
